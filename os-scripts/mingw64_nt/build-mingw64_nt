#!/usr/bin/env bash
set -euo pipefail
REPO_ROOT="$(cd "$(dirname "$0")/../.." && pwd)"
if [ -f "$REPO_ROOT/build-mingw64_nt" ]; then
  exec bash "$REPO_ROOT/build-mingw64_nt" "$@"
else
  echo "build-mingw64_nt not found in repo root" >&2
  exit 1
fi
