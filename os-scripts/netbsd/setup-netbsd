#!/usr/bin/env bash
set -euo pipefail
REPO_ROOT="$(cd "$(dirname "$0")/../.." && pwd)"
if [ -f "$REPO_ROOT/setup-netbsd" ]; then
  exec bash "$REPO_ROOT/setup-netbsd" "$@"
else
  echo "setup-netbsd not found in repo root" >&2
  exit 1
fi
