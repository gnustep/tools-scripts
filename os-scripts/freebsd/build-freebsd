#!/usr/bin/env bash
set -euo pipefail
REPO_ROOT="$(cd "$(dirname "$0")/../.." && pwd)"
if [ -f "$REPO_ROOT/build-freebsd" ]; then
  exec bash "$REPO_ROOT/build-freebsd" "$@"
else
  echo "build-freebsd not found in repo root" >&2
  exit 1
fi
