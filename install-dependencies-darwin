#!/bin/sh
# This file was migrated to os-scripts/darwin/install-dependencies
# The canonical script now lives at:
#   tools-scripts/os-scripts/darwin/install-dependencies
# To run it:
#   ./tools-scripts/os-scripts/darwin/install-dependencies
# To remove this file locally after pulling the branch, run:
#   git rm tools-scripts/install-dependencies-darwin
#   git commit -m "Remove top-level darwin install-dependencies (migrated to os-scripts)"
REPO_ROOT="$(cd "$(dirname "$0")" && pwd)"
exec "$REPO_ROOT/os-scripts/darwin/install-dependencies" "$@"
