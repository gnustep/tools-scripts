#!/bin/sh

#!/usr/bin/env bash
set -euo pipefail
REPO_ROOT="$(cd "$(dirname "$0")/../.." && pwd)"
if [ -f "$REPO_ROOT/post-install-darwin" ]; then
  exec bash "$REPO_ROOT/post-install-darwin" "$@"
else
  echo "post-install-darwin not found in repo root" >&2
  exit 1
fi
