#!/usr/bin/env bash
set -euo pipefail
REPO_ROOT="$(cd "$(dirname "$0")/../.." && pwd)"
if [ -f "$REPO_ROOT/setup-openbsd" ]; then
  exec bash "$REPO_ROOT/setup-openbsd" "$@"
else
  echo "setup-openbsd not found in repo root" >&2
  exit 1
fi
